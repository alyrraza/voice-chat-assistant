[project]
name = "voice-chat-assistant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "groq",
    "requests",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "api.index:handler"

[tool.pytest.ini_options]
pythonpath = ["."]
